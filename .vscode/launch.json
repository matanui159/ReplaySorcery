{
   "version": "0.2.0",
   "configurations": [
      {
         "name": "GDB",
         "type": "cppdbg",
         "request": "launch",
         "program": "${workspaceFolder}/bin/replay-sorcery",
         "cwd": "${workspaceFolder}",
         "preLaunchTask": "Ninja"
      }
   ]
}
